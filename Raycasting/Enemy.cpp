#include "Enemy.h"


Enemy::Enemy(float x, float y, float width, float height, Texture& tex, const std::string& typeName = "default", float z = 0.0f) : Prop(x, y, width, height, tex, typeName, z) {

}